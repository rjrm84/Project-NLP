import uiChecker  from '../src/client/js/uiChecker'

describe("Testing the URl validation  functionality", () => {
    test("Testing the uiChecker() function", () => {
            expect(uiChecker).toBeDefined();
        }
    )})
