import  urlValidation  from '../src/client/js/formHandler'

describe("Testing the UI update functionality", () => {
    test("Testing the urlValidation() function", () => {
            expect(urlValidation).toBeDefined();
        }
    )})
